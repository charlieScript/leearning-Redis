# leearning-Redis
A repository in learning redis

**Redis is a an in memory no sql database that uses key values pairs it doesnt't data, it is a temporary data stire used for caching** 

## commands
- **redis-server**: starts the redis server
- **redis-cli**: accesses the redis cli
- `SET KEY VALUE`: SET name charles
- `GET KEY `: get name 